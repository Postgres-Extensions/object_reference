# object_reference — Claude Code instructions

See [`../ai/CLAUDE.md`](../ai/CLAUDE.md) (or
https://github.com/Postgres-Extensions/ai/blob/main/CLAUDE.md if that path
doesn't exist locally — clone it to `../ai/` per its own first instruction)
for CI-monitoring and other cross-repo conventions, and `../ai/PR.md` for
PR conventions including the `CI: ` title-prefix rule.

This repository runs GitHub Actions CI via `.github/workflows/ci.yml` on
every push and pull request.
